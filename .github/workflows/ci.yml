name: CI
on: [push, pull_request]
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt
      - run: python -m pytest -q
      - run: pip install pip-audit
      - run: pip-audit -r requirements.txt
  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - uses: docker/build-push-action@1dc73863535b631f98b2378be8619f55e2c9f
        with:
          context: .
          push: false
          tags: secure-auth-backend:test
  compose-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt
      - run: docker compose up -d postgres redis
      - run: |
          for i in $(seq 1 30); do
            docker compose exec -T postgres pg_isready -U auth -d auth && \
              docker compose exec -T redis redis-cli ping && exit 0
            sleep 2
          done
          docker compose logs
          exit 1
      - env:
          DATABASE_URL: postgres://auth:auth@localhost:5432/auth
          REDIS_URL: redis://localhost:6379/0
          SECRET_KEY: compose-test-secret
        run: python -m pytest -q
      - run: docker compose down -v
