# Contributing

Create focused changes, add regression tests for security behavior, and run
`python -m pytest -q` before opening a pull request. Never commit `.env`,
database files, credentials, raw session values, or password-reset tokens.
Production configuration must remain fail-closed.
